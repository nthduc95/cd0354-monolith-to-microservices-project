apiVersion: v1
kind: Service
metadata:
  name: backend-user-service
  labels:
    app: backend-user-service
spec: 
  ports:
    - name: "8080"
      port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: backend-user-service